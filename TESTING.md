# 测试交接

本文档面向接手测试的人/agent。前置上下文为零，按本文操作即可。

被测对象：`fork/main` 上相对上游基线 `9fab79d` 的全部提交。

```bash
git log --oneline 9fab79d..HEAD
git diff 9fab79d..HEAD --stat
```

---

## 0. 环境要求（不满足会得到误导性的失败）

| 依赖 | 要求 | 检查 |
|---|---|---|
| PowerShell | **Windows PowerShell 5.1**，不是 pwsh 7 | `$PSVersionTable.PSVersion` / `$PSVersionTable.PSEdition` 必须是 `Desktop` |
| csc.exe | .NET Framework v4.0.30319 | `%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe` |
| WPF | PresentationFramework 在 GAC | 构建安装器需要 |

pwsh 7 跑构建脚本会炸：它解析出的压缩程序集是 .NET Core 版路径，喂给 Framework csc 不兼容。

**跑自动化测试之前先把软件关掉。** 这一条被我自己绊了两次，所以单独写出来：

| 测试 | 软件开着时 | 为什么 |
|---|---|---|
| `engine-host-session-tests` | `test could not acquire the current-session launcher marker` | 它要新建 `Local\DeltaForceBooster.LaunchInstance`，而运行中的软件正占着这个全局单实例标记 |
| `installer-security-tests` | `fresh install exit=3` | 安装器检测到旧版仍在运行，**主动取消了安装**（`setup-wizard.cs:197/205`）—— 这是保护机制在正常工作，不是缺陷 |

确认办法（两个都应该是 `True`）：

```powershell
foreach ($n in 'Local\DeltaForceBooster.LaunchInstance', 'Global\DeltaForceBooster.Engine') {
  $created = $false
  $m = New-Object Threading.Mutex($false, $n, [ref]$created)
  '{0,-42} 可新建={1}' -f $n, $created
  $m.Dispose()
}
```

`False` 就说明软件还开着（或上一次没退干净），先关掉再跑，否则这两条必红而且和你的改动无关。

---

## 1. 自动化测试

```powershell
Get-ChildItem tests\*.ps1 | Sort-Object Name | ForEach-Object {
  & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $_.FullName 2>&1 | Out-Null
  '{0,-34} {1}' -f $_.Name, $(if ($LASTEXITCODE -eq 0) { 'PASS' } else { 'FAIL' })
}
```

**期望：21 个文件全部通过。**（`restore-accounting-tests.ps1` 随「还原记账」新增，见 2.6；
`symptom-entry-tests.ps1` 随「按症状入口」新增，见 2.8）

但有个前提 —— 下面两个用例读的是构建产物，不先出测试包必定失败，跟改动无关：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build\make-installer.ps1 -TestBuild
```

| 文件 | 前置条件 |
|---|---|
| `installer-security-tests` | 需要上面那行先产出测试包 |
| `installer-drive-picker-tests` | 同上 |

新增的 `startup-bootstrap-tests.ps1` 必须通过。

`notification-feature-tests.ps1` 已随通知功能一并删除 —— 它读 `server/report_server.py`，
而上游把服务端代码转私有后这个文件在公开仓库里根本不存在，本来就不可能通过。

---

## 2. 需要真机验证的部分（自动化测不到）

GUI 脚本**不能直接运行** —— `gui/DeltaForceBooster-GUI.ps1` 开头有硬闸门，要求 7 个环境变量、父进程 PID 等于 EngineHost、`%TEMP%` 精确匹配会话目录。要看效果必须走完整构建：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File build\make-installer.ps1
```

> 注意：`-TestBuild` 会在仓库根留下带 `DFB_TESTING` 编译开关的启动器（认 `DFB_TEST_SKIP_ACL=1` 旁路）。
> 验证完请重跑一次**不带** `-TestBuild` 的构建。

### 2.1 零后端（提交 804e114 / 40e3b15 / edf4117 / 本次）

上游有四个网络出口：遥测上报、60 秒通知轮询、诊断上传、更新检查。
前三个已经整条删除，第四个改指 GitHub Releases。**这一节是本次改造最该被真机验证的部分。**

1. **抓包（最关键）**：装好后全程操作 —— 检测、执行优化、还原、导出诊断报告、
   开关自动调优 —— 全程抓包。**除了 `github.com` 与 `*.githubusercontent.com`
   之外不应有任何出站请求**；尤其不得出现 `upstream-host.invalid`
2. 界面上「运行日志」页底部应是一句**说明文字**（本软件不收集、不上报任何使用数据…），
   不再是复选框
3. `%ProgramData%\DeltaForceBooster\users\<SID>\config\` 下：
   - 不应再生成 `telemetry.json` 或 `tuning-telemetry-outbox.json`
   - 执行一次优化后应出现 `optimization-context.json`，内容里**不得含**
     `InstallId` / `DeviceToken` / `TokenExpiresAt` / `Enabled`
4. 安装目录 `scripts\` 下不应有 `telemetry-client.ps1`
5. **导出诊断报告**：点「导出完整诊断」→ 选问题/改善 → 确认。
   报告应写到**桌面**（OneDrive 重定向过桌面的机器请重点看这条），
   运行日志里应打印完整路径；文件用记事本打开中文不乱码
6. **检查更新**：点「检查更新」。当前 GitHub 上还没有 release，
   预期是静默无更新（不得报错、不得影响主界面）。
   等真正发过一版后再回来验完整的下载 → 校验 → 安装链路

> ⚠️ **GitHub 在国内可能连不上。** 如果检查更新长时间无响应，先确认是网络问题
> 而不是代码问题 —— 用浏览器打开
> `https://github.com/FUDAHA99/FrameRateHelper/releases/latest/download/update-manifest.json`
> 对照。下载失败时界面必须退回「提示 + 跳下载页」，这是设计行为。

### 2.2 启动引导日志（提交 935950f）

1. 正常启动一次 → `%ProgramData%\DeltaForceBooster\startup-logs\` 下应出现
   `startup-<时间戳>-<pid>.log`，内容含 PS 版本、CLR、系统版本、区域设置、环境变量
2. 该文件应为 **UTF-8 with BOM**，中文不乱码
3. 会话标记应显示为 `SESSION=xxxxxxxx…`（**只有前 8 位**，不是完整 32 位十六进制）
4. **制造失败**：直接用 `powershell -File gui\DeltaForceBooster-GUI.ps1` 运行（绕过启动器）。
   期望：弹窗给出**可行动的建议**（「这通常是直接双击了 .ps1 脚本…请改用启动优化工具.exe」），
   并显示日志路径；同时日志里有 `!! 启动被拒：` 那一行
5. 目录应对普通用户**可读**（非管理员账户能打开）

**最该重点验的一条回归**：在一台**从未装过**本软件的干净机器（或先删掉
`%ProgramData%\DeltaForceBooster`）上安装并首次启动。必须能正常打开。

> 背景：本层第一版会在最顶端创建那个受保护根目录，导致它继承
> `Owner=当前用户` + `BUILTIN\Users:Write`，随后 `New-ProtectedDirectory`
> 抛「已拒绝接管」，**全新安装的机器每次都打不开**。已修复并加了断言，
> 但这是本次改动里唯一能造成灾难性后果的地方，值得真机确认。

### 2.3 独立诊断导出器（提交 935950f）

```
双击安装目录下的  导出诊断信息.cmd
```

1. **以普通用户身份**运行（不要右键管理员）—— 必须能跑完
2. 桌面应生成 `帧率优化助手-诊断-<时间戳>.txt`
3. 内容应包含：运行环境、安装状态、受保护数据目录（含 ACL）、启动日志、
   相关进程、安全软件、系统事件日志七节
4. 故意破坏：把安装目录改名，再跑一次 —— 应照常生成报告并在「安装状态」里标出缺失文件，
   **不应抛异常**
5. 确认报告里**没有**账号密码、注册表内容、游戏路径

---

### 2.4 显示层改名（提交 39ec368 / 本次）

ASCII 标识符 `DeltaForceBooster` 与全部派生身份**没有改**，只改了用户看得见的名字。
`tests/identity-freeze-tests.ps1` 钉住了这条边界（80 条断言）。

1. **窗口标题**：主窗口和各对话框标题栏应显示「帧率优化助手」；顶栏左上角是
   「▲ 帧率优化助手 | 为《三角洲行动》优化 [ v0.23.0.13 ]」
2. **UAC 同意框**（反钓鱼，重点看）：点「执行优化」弹出的 UAC 框上，
   程序名应是「帧率优化助手 管理员助手」—— 必须和用户刚点的那个窗口能对应上。
   安装器的 UAC 框应是「帧率优化助手 安装向导」
3. **快捷方式**：桌面和开始菜单里是「帧率优化助手」，开始菜单目录名也是「帧率优化助手」
4. **安装向导**：欢迎页标题「欢迎安装 帧率优化助手」，
   「安装前请知悉」第一条应是免责声明（与腾讯及《三角洲行动》官方无关）——
   上游版本这一页完全没有免责声明，这是新加的
5. **文件属性**：右键 exe → 属性 → 详细信息。
   「文件说明」是新名字，「产品名称」仍是 `DeltaForceBooster` —— **这是故意的**，
   `setup-wizard.cs:1918/2100` 拿 PE 的 ProductName 做身份比对，改了会让存量用户
   的覆盖安装永久失败

#### 必须验的两个升级场景

| | 场景 | 必须看到什么 |
|---|---|---|
| **G** | **旧版正在运行时跑新安装器** | 必须真的关掉旧窗口，或明确提示「仍有旧版拒绝退出」。**绝不能**静默判定「没有旧实例」继续覆盖 —— 旧引擎可能正在改系统，覆盖等于把系统改到一半且备份写不完整。这条走的是 setup-wizard.cs 的新旧标题双匹配 |
| **H** | 旧版装过 → 覆盖安装新版 | 桌面和开始菜单**只有一份**图标（新名字）；旧的「三角洲行动优化助手」图标应已被清理。注意：清理只删「目标指向本安装目录」的，所以如果你手工复制过一份指向别处的同名快捷方式，它会被保留 —— 这是设计行为 |

#### 为什么不做全量改名

`%ProgramData%\DeltaForceBooster` 那棵树是用户回退系统改动的唯一依据，
而且卸载时永久保留。改名会让存量用户**静默**失去回退能力：

- `delta-booster.ps1:2993` 的 sched op 白名单逐字校验 `DeltaForceBooster-PowerPlanLock`，
  改前缀 → 含该 op 的历史备份整份 throw → 叠加 `ForEach-Object` + `Stop` → 整页还原报错
- `delta-booster.ps1:3187` 只让**无签名**的旧格式备份走 legacy-roots 那条路；
  现版本的签名备份在受保护目录之外是直接 throw
- `backup.key` 与备份同根

失败模式是肯定式的安抚：优化页读实时系统值照常显示「已优化」，
还原页平静地说「当前没有仍由工具管理的可还原改动」。

### 2.5 还原的影响范围与可发现性（提交 cc69343）

这一条比界面好不好看重要得多：还原是这个产品的安全内核，用户敢用它改系统，
唯一的依据就是"改坏了能回退"。

**造坏数据的办法**（需要管理员权限；先备份整个目录再动手）：

```powershell
$bk = "$env:ProgramData\DeltaForceBooster\backup"
# 先做几次真实优化，让目录里有 3 份以上备份，然后：
# ① 篡改其中一份：随便改个字节，不重算 HMAC
# ② 再放一份垃圾：
'this is not json' | Set-Content "$bk\backup-00000000-0000-0000-0000-000000000000.json"
```

1. **一份坏的不得株连其余**：点「还原设置」，列表**仍应列出**其他完好备份对应的项目。
   原先的行为是整页报错
2. **绝不静默**：面板上方的提示区应逐条列出「已跳过读取失败的备份 <文件名>：<原因>」，
   并有一句「有 N 份备份无法读取…请勿删除备份文件」
3. **空列表必须说清楚搜过哪里**：把 backup 目录整个清空后再点还原，
   空态文案应是「当前没有支持按项目精确复原的记录。已搜索：<完整路径>」。
   用户据此才能分辨「确实没改过」和「改过但工具没找到」
4. **指名还原一份坏备份**要如实报错（不是「没找到备份」）—— 这条走命令行更好验：
   `powershell -File scripts\delta-booster.ps1 -Action Restore -BackupFile <那份坏的>`
5. **全部读不了 ≠ 一份都没有**：只留下坏备份，点「全部复原」，
   错误信息必须说「找到 N 份备份，但全部无法读取」，不能说「未找到任何备份文件」

**legacy-roots 那条**：往 `%ProgramData%\DeltaForceBooster\legacy-roots.json` 的
`Roots` 数组里插一条垃圾（比如 `"not-a-path"`），还原入口必须照常打开，
并把那条报成警告。原先的行为是整个还原子系统抛异常。

> 这一类问题（一条坏数据打掉整个功能）在这个仓库里已经出现三次了。
> 复查时请顺手想一下你看到的每个 `foreach` / `ForEach-Object` 里，
> 单条失败会不会掀翻整批。

### 2.6 还原记账：部分失败不得连累其余（提交 2f2e675）

改之前，还原的记账是一个全局闸门：**只要有一条改动失败，这一整次还原就一笔都不入账**。
后果有两个方向，都很严重：

- 13 条已经写回原值的改动不被记录 → 用户下次再点还原，工具会把同样的旧值**再写一遍**，
  覆盖他在这两次还原之间做的手动修改；
- 同时「需要重启才生效」的提示被整体清空 → 用户以为不用重启。

现在改成一条改动一笔账。真机怎么造这个场景：

1. 跑一次完整优化，让备份里有十几条改动。
2. **人为让其中一条还不回去**：最省事的办法是把某个注册表键的权限改成拒绝写入
   （`regedit` → 找到该键 → 权限 → 给 Administrators 拒绝「设置数值」），
   比如 `HKLM:\SYSTEM\CurrentControlSet\Control\PriorityControl` 的 `Win32PrioritySeparation`。
3. 点「全部复原」。**期望**：对话框说「有 1 项还原失败」，其余各项都实际写回了原值；
   需要重启的项目**仍然弹出重启提示**（改之前这里什么都不弹）。
4. 把权限改回去，**再点一次「全部复原」**。这一步是重点：
   - 进度条应只处理**那一条**，不是重新跑十几条；
   - 事先在还原之间手动改一个已经还原成功的值（例如把 `HwSchMode` 改成 3），
     第二次还原后它必须**保持 3**。改之前这里会被旧值覆盖回去。
5. 命令行核对：`powershell -File scripts\delta-booster.ps1 -Action Restore -Json`
   的结果里，`RestoredItemIds` 只包含**全部改动都写回了**的项目；只还原了一半的项目
   不在其中，但它的名字会出现在 `SkippedItemIds` 或 `Failed` 里。

**记账失败是单独一档**：把 `%ProgramData%\DeltaForceBooster\backup` 目录设成只读再点还原。
系统设置会正常写回，但凭证写不进去。**期望**：标题是「还原部分完成」而不是「还原完成」，
正文明确说「不要立刻重试还原」，退出码是 `6`（不是 `4`）。
改之前这里会说成「有 N 项还原失败，对应改动仍留在系统中」——那是假话，改动已经写回去了。

**计划任务那条（需要真机，自动化只能到 stub 为止）**：还原「锁定电源计划」时，
工具会先问任务计划服务「这个任务还在不在」。把 `Task Scheduler` 服务停掉再点「全部复原」：

```powershell
Stop-Service Schedule -Force
```

**期望**：明确报「无法确认计划任务 … 是否仍存在」并**失败**，同时电源方案那一项也不算
还原成功（理由是「刚写回的电源方案会在 1 分钟内被它改回优化方案」）；**不写消费凭证**。
改之前这里会把查询失败读成「任务已经不在了」，记成功、写凭证，而任务还活着，
每分钟把刚还原好的电源方案切回去，「优化」页还一直显示「未锁定」。
把服务启回来再点一次，两项都应该正常还原。

顺带验一下换过安装目录的情况：工具的锁定任务名带一截安装根哈希。把软件装到另一个
目录再打开，「优化」页对「锁定电源计划」的状态判断应该仍然认得出旧任务（显示已锁定），
而不是显示「未锁定」诱导你再点一次优化——那一点必然失败，因为同名任务已存在。

**冲突判定那条**：还原清单里的「发生后续修改」以前是两态判的——只问「当前值还等于
工具写进去的值吗」，不等于就一律说你改过。于是还原跑到一半断电、或者某条改动失败之后，
**已经还原好的**项目会变红字「检测到优化后又被用户或其他程序修改」，复选框灰掉，而且
再也恢复不了（凭证只在成功时才写）。手动验：

1. 优化 → 打开还原清单 → 勾一项 →「复原所选项目」，**在进度条走完前强杀进程**
   （任务管理器结束 `启动优化工具.exe` 和 `EngineHost.exe`）。
2. 重新打开还原清单。**期望**：那一项显示「已恢复原值」且**可勾选**，
   点一次复原后它从清单里消失。改之前这里是红字 + 灰掉，永久。
3. 真正的第三方修改仍然要被拦住：手动把某个被优化过的注册表值改成**第三个值**
   （既不是原值也不是工具写的值），该项应显示「发生后续修改」，`Reason` 里
   **逐条列出是哪个设置**；同项目里没被你改过的设置仍然可以复原，复原后你改的那个
   值**保持不变**。全部复原也一样——那一条会进「跳过」而不是被旧值覆盖。

**那个会把人带进死路的「重要提醒」**：升级后第一次打开软件会强制弹一次
「优化后出现异常：先恢复电源选项并重启电脑」。它原来教用户「进还原设置 → 勾选三个电源项
→ 复原所选项目」，而这三项根本不在按项目复原的白名单里——面板上连行都不会出现，
按钮因为一个都没勾而**禁用**。看到这个弹窗的正是已经出问题的用户。
删掉 `%ProgramData%\DeltaForceBooster\users\<你的 SID>\config\version-notice-v0.23.0.8.json` 再开一次软件
（这个目录的 ACL 只授权 Administrators 和 SYSTEM，要用管理员身份的资源管理器或命令行删；它**不在** `%LOCALAPPDATA%`），
**期望**：文案改成「点击面板下方的『全部复原』」，并解释了为什么不是「复原所选项目」。

**结论三态**：还原完成后的对话框现在有三个标题。
「还原完成」只允许在**没有失败、没有跳过、没有读不了的备份、没有还不回去的改动**时出现；
其余情况是「还原部分完成」；有失败才是「还原未完成」。
最容易验的一种：把某个被优化过的注册表值改成第三个值，再点「全部复原」——
那一项会进「跳过」，标题应是「还原部分完成」，弹窗里**直接列出项目名**（不是「明细见运行日志」）。

**运行日志**：打开还原清单的那一刻，运行日志里就该出现 `[还原清单] …` 开头的行
（读不了的备份、不可选的项目及原因）。改之前这个函数一次 `Write-Log` 都没调过，
而面板和引擎的文案都写着「见运行日志」——那里什么都没有，导出的诊断报告里也没有。

**电源方案那条**：如果机器上装了第三方电源管理器（Armoury Crate、机型控制中心），
它可能拦下 `powercfg /setactive`。此时还原会退到 Windows「平衡」并提示，
但因为原方案**还在**，这条记录**不会被消费**——关掉那个软件再点一次还原，
应该能精确切回原方案。反过来，如果原方案已经被删掉了，它必须一次就消费掉，
不能让用户每点一次还原都看到同一句失败。

### 2.7 工具残留反查（提交 4af7401）

还原「成功」之后，系统里仍然留着四类以本产品命名的东西，用户在控制面板和任务计划
程序库里看得到名字，却不知道能不能删、怎么删；而还原成功会消费掉备份，此后连 GUID /
任务名都不可达。「优化」页新增了「检查工具残留」按钮。

1. 跑一次包含「电源计划切换到卓越性能」和「锁定电源计划」的优化，再「全部复原」。
2. 点「检查工具残留」。**期望**至少列出：工具自建的电源方案（可清理）、
   受保护数据目录（**仅提示，不可清理**，并说明它的权限只授予管理员和 SYSTEM）。
3. 把软件装到**另一个目录**再执行一次「锁定电源计划」，然后回到原目录的软件点检查。
   **期望**：那个孤儿任务被列出来（任务名带另一截哈希），并且能删掉。
4. 正在使用工具电源方案时点检查，**期望**那一条显示为不可清理并说明原因；
   切换到别的方案后再点，它应该变成可清理。

**IFEO 空键**：如果执行过 `game-priority`，还原后
`HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\`
下面那个以游戏主程序命名的空键应该已经被自动删掉（运行日志里有「已清理还原后残留的
空注册表键」）。这个位置是杀毒软件和反作弊的重点扫描位，留一个以游戏主程序命名的
空键，对用户和任何安全审计都是无法解释的。

**卸载器**：只打开过一次软件（没做过任何优化）就卸载，**期望**摘要里仍然出现
「受保护数据目录保留在：…」并说明普通账户删不掉。改之前这种情况只会看到
「未发现备份记录」，而盘上留着一棵自己删不动的目录树。

---

---

### 2.8 按症状入口（提交 c9280d8）

优化项名写的全是手段（`HwSchMode`、MMCSS、`DisablePagingExecutive`），用户找的是症状。
「优化项」列表上方新增了一条**症状筛选带**，19 条症状与诊断报告的「当前问题」多选项
是同一份数据（引擎 `Get-SymptomCatalog`）。

**它是纯呈现层：不写系统、不碰备份、不经过提权。** 所以这一节要打的不是安全边界，
而是三件会让用户被误导的事。

1. 打开软件，看「优化项」上方那条带子。**期望**：19 个可点的症状；下面的表格有表头
   （优化项 / 当前状态 / 重启 / 优化状态），而且每一行的列都和表头对齐。
2. 点「掉帧 / 帧率波动」。**期望**：列表只剩相关项目，金色方框里给出这条症状的说明，
   并出现「去『掉帧修复』页」按钮；计数条变成「已选 N · 筛选内 M / 32」。
3. **最要紧的一条**：先点「显示全部」，手动勾上「禁用 Windows Search 索引服务」，
   再点「输入延迟高 / 操作粘滞」（这条症状与 Windows Search 无关）。
   **期望**：那一行**仍然留在屏幕上**，并带着金色的「不在筛选内」标记，摘要里也会说
   「另有 1 项已勾选但与所选症状无关」。
   如果它被藏起来了，就是个必须打回的缺陷：点「执行优化」会往系统里写一个用户在屏幕上
   看不见的项目。
4. 保持某条症状选中，点「全选」。**期望**：只有**看得见的**那几项被勾上，列表外的项目
   一个都没被带上（计数条的「已选」应等于屏幕上勾选框的个数）。
5. 保持某条症状选中，从「预设方案」里套一个方案。**期望**：方案里那些原本被筛掉的项目
   **自动变回可见**并带「不在筛选内」标记 —— 同一条不变式，从另一条路径进来。
6. **先点「全选」把勾去掉（点两下，让一个都不勾）**，再点「CPU 占用或温度过高」。
   **期望**：列表空掉，并明确写着「所选症状在本工具里没有对应的优化项」，金色方框里说明
   **电源类优化本身就是用更高功耗换性能**，并给出「打开复原入口」按钮。
   为什么要先清空勾选：默认启动会套用「主推全套」，十几项是勾着的，而**勾着的行永远不会被
   筛选隐藏**（第 3 步那条不变式）。所以不清空的话列表不会空，你看到的是那十几项带着
   「不在筛选内」标记留在屏幕上 —— 那也是对的，顺手确认一下。
   这条是刻意的：本工具没有降温项，把「卓越性能」列在「温度过高」下面等于推荐用户去做
   让症状更重的事。`风扇噪音大 / 功耗高`、`GPU 占用或温度过高` 同理。
7. 鼠标悬浮在任意一项的**项名**上。**期望**：提示里有三行 ——「现在：」当前状态、
   「执行后：」会变成什么、「重启：」要不要重启。体检项应该显示「本项只读：……」
   且**不谈重启**，行尾还有一个「只读」徽标。
8. 找一个「当前状态」被省略号截断的项（`power-tuning` 一般都是），鼠标悬浮上去。
   **期望**：弹出**完整的多行**内容，一行一个底层设置。社区那条真实 issue 说的就是
   这里看不全。
9. 点「导出完整诊断」。**期望**：第一页那 19 个「当前问题」选项与筛选带上的 19 条**逐字
   一致** —— 它们现在是同一份数据。

**命令行同一份数据**：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\delta-booster.ps1 -ListSymptoms
```

**期望**：19 条症状，每条列出对应优化项与去处；没有对应优化项的**五条**（CPU 温度过高 / GPU 温度过高 / 风扇噪音大 / 优化工具打不开 / 优化或还原执行失败）明确写「无（本工具没有能
直接解决这条症状的优化项）」；输出里**不应**出现任何「症状目录异常」。
如果出现了，说明目录里有一个 Id 拼错了 —— 那条症状会静默地筛不出任何东西，表现得像
「本工具治不了这条」。

**列宽拖动没有做**，这是刻意的：社区那个外部 PR 提的三件事里，补表头和悬浮完整内容都
做了，而拖列宽本来就是为了对抗截断，截断已经被悬浮正面解决了。

---

### 2.9 分支归属与双版权（提交 3079a50）

本项目是 Leonard8818 那个仓库的分支。MIT 要求保留上游版权声明，本分支自己的修改也要
署名，所以 `LICENSE` 里是两行版权，发布二进制的「版权」字段也写了两个人。

**这一节真正的风险不在文案，在一个容易被顺手改掉的身份闸门。**

`AssemblyCompany("DeltaForceBooster 开源项目")` 看起来像品牌文案，实际上
`setup-wizard.cs` 用 `StringComparison.Ordinal` 把 PE 的 CompanyName 与它逐字比对（两处），
不一致就拒绝把那个文件当成本产品的文件。它原来**不在冻结清单里** —— 本次一并钉住。
失败发生在**安装阶段**，本地构建-运行一遍根本碰不到。

1. 出一次正式包（不是 `-TestBuild`），右键 `DeltaForceBooster-Setup-vX.Y.exe` →
   属性 → 详细信息。**期望**：
   - 版权 = `MIT License · Copyright (c) 2026 Leonard8818, FUDAHA99`
   - 产品名称 = `DeltaForceBooster`（**没变**，这是安装身份闸门）
   - 公司 = `DeltaForceBooster 开源项目`（**没变**，同上）
   - 文件说明 = `帧率优化助手 安装向导`
   `启动优化工具.exe`、`EngineHost.exe`、卸载助手同理。
2. **在装有旧版本的机器上做一次覆盖安装。** 这是上面那两个「没变」唯一的真实检验：
   身份闸门若被改动，表现是安装向导不认识已装的版本，或者 D 盘 anchor 识别失败。
3. 卸载后重装一次，确认受保护数据目录里的旧备份仍能被新版本读到。

**文档侧**（`tests/identity-freeze-tests.ps1` 已经断言，这里只是说明为什么）：
`README.md` 和 `CONTRIBUTING.md` 里不允许再出现 `数据接收服务`、`运营看板`、
`upstream-site.invalid` —— 那是上游的服务端布局和官网，本分支一个服务端都没有，留着就是在说假话。

---

### 2.10 执行期间界面必须是「锁住」的（提交 ae91899）

这一节的每一条都**只在真实的提权往返中才现形**，自动化测不到：等提权子进程时跑的是
DoEvents 轮询，而它原来用的是 `DispatcherPriority::Background` —— 低于 Input，
所以待处理的鼠标/键盘事件会被派发进来，界面是活的。

1. **启动扫描还没结束就点「执行优化」。** 开软件，在状态还是「检测中…」、日志还没打出
   「检测完成」之前，用力点几下「执行优化」。**期望**：点不动（按钮禁用），日志里也不会
   出现第二轮 Apply。改之前那段时间 `$script:Busy` 全程是 false，一次完整的 Apply 能嵌套
   在启动处理器内部跑完，跑完之后启动代码还会把「主推全套」重新勾满整张表 ——
   屏幕上显示「已默认选中主推全套、还没执行」，而系统已经被改过一轮。
2. **执行优化进行到一半时去点勾选框和「全选」。** 勾若干项 → 「执行优化」→ 确认 →
   趁进度条在走（电源/注册表那批可能持续几十秒），点优化项的勾选框、点表头的「全选」、
   按 Tab 再按空格。**期望**：全都点不动。改之前这些控件是启用的，点一下就会让被取消
   勾选、又与当前症状筛选无关的那一行**当场从屏幕上消失**，而引擎正在把它写进系统。
3. **确认框弹出期间改勾选。** 勾 A 和 B → 「执行优化」→ 在「确认执行」对话框还开着的
   时候（模态框自己会跑消息泵，主窗口点得到）回主窗口把 B 取消 → 回到对话框点「执行优化」。
   **期望**：弹出「勾选已变化 / SELECTION CHANGED」，本次不做任何改动。
   改之前 `$ids` 是在四个确认框**之前**采的，B 会照写不误。
4. 执行期间点「检查工具残留」「症状筛选带的去处按钮」「显示全部」。**期望**：都不响应，
   或在日志里如实说「正在执行优化/还原」。

**为什么不只靠禁用控件**：`RaiseEvent` 根本不看 `IsEnabled`，而普通的界面刷新函数
（`Update-SymptomFilterUi`）能把 `Set-BusyState` 的禁用逐项撤销回去。所以现在是三层：
禁用清单 + 按钮自己的 `$script:Busy` 闸门 + 置忙后重新核对勾选快照。

**顺带看一眼列宽**：把窗口横向拉窄到大约一半。**期望**：长项名被裁成省略号（鼠标悬浮
能看到全名），**不会**压到「当前状态」列上。改之前 560px 时 32 行里有 18 行会压过去。

---

### 2.11 窗口本身与「界面对你做的那个断言」

这一节是复核那一轮收尾时确认的六条。前三条关于窗口，后三条关于界面在屏幕上对用户
做出的承诺 —— 承诺不成立的时候，界面必须自己撤回。

1. **在小屏上最大化。** 找一台工作区高度不足 640 的机器（1024x600 上网本、竖屏，
   或者 1366x768 开 150% 缩放），点标题栏的最大化。**期望**：窗口正好贴住工作区，
   底部那一整行主操作按钮（执行优化 / 一键还原 / 导出报告）都在屏幕内。
   改之前 XAML 里写死的 `MinHeight=640` 会盖过工作区约束，窗口比工作区还高、
   按钮掉到屏幕下方 —— 而最大化状态下用户没法拖窗口把它拽回来。
2. **最大化之后拖标题栏。** 最大化，把鼠标按在标题栏**靠右**的位置往下拖。
   **期望**：窗口还原，并且光标仍落在标题栏上（大约仍是靠右那个相对位置），
   拖拽连续不断。改之前窗口会按还原前的旧坐标落回去，光标可能整个不在窗口上，
   手感是「窗口被甩走了」。多显示器下在副屏各做一次。
3. **看最大化按钮的图标。** 最大化前是一个方框，最大化后应该变成两个叠起来的方框
   （「向下还原」），提示文字也跟着变。改之前用的是 `☐`/`❐` 两个字符 ——
   这两个码位在微软雅黑里**都没有字形**（实测 `CharacterToGlyphMap` 里都不存在），
   靠字体回退很可能两态落到同一个方框上，用户看不出自己在哪个状态。
4. **套一个方案，然后点「重新检测」。** 选「★ 主推全套」（27 项被勾上）→ 点「重新检测」。
   **期望**：下拉恢复成未选中、方案说明清空，日志里写一句「原先选中的方案已取消」。
   改之前下拉会继续写着「★ 主推全套」，而整张表已经被重建、勾选回到默认集（17 项），
   10 项静默消失 —— 用户照着下拉的字执行，得到的不是它承诺的东西。
   顺带确认**启动时**的自动套用没被这条误伤：开软件，检测完成后「★ 主推全套」应该
   仍然是选中的。
5. **执行期间看症状 chip。** 执行优化进行到一半时看上面那排症状按钮。**期望**：整排
   明显变淡，鼠标移上去是普通箭头而不是手形。chip 是代码手搭的 `Border`，不走任何样式，
   所以样式层那几处禁用态一个也覆盖不到它 —— 改之前它在屏幕上零变化，
   用户点下去没反应，读起来又是一次「卡死了」。
6. **执行期间看「预设方案」下拉。** 同上，**期望**：整个下拉（连同里面选中项的文字）
   变淡。注意它的模板里那个悬浮触发器挂在内部的 `ToggleButton` 上，
   只改那个的话淡的是右边那个箭头，选中项的文字照常是亮的 —— 所以这条要看**文字**。

---

## 3. 我最不确定的地方（请重点打）

1. **`trap` 的副作用**。我在脚本作用域加了 `trap { ...; break }`。理论上它只接
   没被 try/catch 接住的终止性错误，且 `break` 保持原有「出错即终止」语义。
   但这个改动影响面是全局的，请确认没有任何原本能正常走完的流程被它提前中断。

2. **`Save-AppUiPreferences` 的第三个参数默认值**。它默认「读回当前磁盘值」。
   关窗保存（`gui:9348`）和 `Save-AppTheme` 都只传两个参数。
   请确认切换主题、调整窗口大小、关窗这些操作**不会**意外改动统计开关。

3. **`startup-logs` 的 ACL**。它是唯一一个 `$UsersRead = $true` 的受保护子目录。
   请确认普通用户只能**读**、不能写（不能往里面塞文件或改现有日志）。

4. **非中文区域设置**。上游有个提交是
   `367fd75 fix: 非中文区域设置的机器上软件完全无法启动`。
   我新增的代码含中文字符串。请在英文/日文区域设置的 Windows 上验一遍启动与日志。

---

## 4. 明确不在本次范围内

- 「回退到上一个可用版本」按钮：**决定不做**。安装器已在「新版启动失败」时自动回滚
  （`setup-wizard.cs:263-277`），而回滚副本在切换成功后即被删除（`:2743`），
  做用户可点的回退需要改安装事务模型，风险收益比不划算。
- 上游服务端依赖已全部移除：遥测、通知轮询、诊断上传删掉，更新源改为 GitHub Releases。
- 第三方传感器栈与 PawnIO 内核驱动已移除；剩余第三方件只有 Intel MIT 的 PresentMon。
- 产品改名只做了显示层：ASCII 标识符仍是 `DeltaForceBooster`，且**永久冻结**（理由见 2.4 末尾与 tests/identity-freeze-tests.ps1 的文件头）。
